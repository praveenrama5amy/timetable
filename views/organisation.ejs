<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>
        <% if (locals.title != null) { %>
            <%= title %>
        <% } else { %>
            Organisation
        <% } %>
    </title>
    <link rel="stylesheet" href="https://fonts.googleapis.com/css2?family=Material+Symbols+Outlined:opsz,wght,FILL,GRAD@20..48,100..700,0..1,-50..200" />
    <style>
        *{
            box-sizing: border-box;
            margin: 0;
            font-family: system-ui, -apple-system, BlinkMacSystemFont, 'Segoe UI', Roboto, Oxygen, Ubuntu, Cantarell, 'Open Sans', 'Helvetica Neue', sans-serif;
        }
        html{
            height: -webkit-fill-available;
        }
        body{
            height: 100%;
            width: 100%;
            margin: 0;
        }
        .pages{
            margin: auto;
            width: 80%;
            height: 90%;
            border: solid black 1px;
            display: flex;
            flex-direction: column;
        }
        .buttons{
            background-color: #232f3e;
            min-height: 35px;
            display: flex;
            align-items: center;
            /* justify-content: center; */
            position: relative;
        }
        .buttons button{
            display: flex;
            align-items: center;
            justify-content: center;
            cursor: pointer;
            background-color: transparent;
            border: none;
            color: #ffffff;
            padding: 20px 10px;
            font-weight: bold;
            text-transform: capitalize;
            font-size: 16px;
            opacity: 0.9;
        }
        .buttons button:hover{
            opacity: 1;
        }
        .page{
            height: 100%;
        }
    </style>
</head>
<body>
    <h1 style="text-align: center;text-transform: uppercase;letter-spacing: 10px;font-weight: bold;">
        <% if (locals.title != null) { %>
            <%= title %>
        <% } else { %>
            Organisation
        <% } %>
    </h1>
    <div class="pages">
        <div class="buttons">
            <button location="/organisation/<%= title %>/Subjects" onclick="changePage(this)"><span class="material-symbols-outlined">menu_book</span>Subjects</button>
            <button location="/organisation/<%= title %>/Tutors" onclick="changePage(this)"><span class="material-symbols-outlined">groups</span>Tutors</button>
            <button location="/organisation/<%= title %>/Classes" onclick="changePage(this)"><span class="material-symbols-outlined">meeting_room</span>Classes</button>
            <button location="/organisation/<%= title %>/summary" onclick="changePage(this)"><span class="material-symbols-outlined">meeting_room</span>Summary</button>
        </div>
        <div class="page">
            <embed id="page" src="/organisation/<%= title %>/Classes" type="text/html" style="height: 100%;width:100%;">
        </div>
    </div>
    <script>
        function changePage(e){
            let location = e.getAttribute("location");
            let embed = document.getElementById("page")
            embed.src=location
        }
        function navigateTo(url){
            location.assign(url)
        }
    </script>
</body>
</html>